//scss
import "../../sass/_listen.scss"

const Listen = () => {
    return (
        <div className="listen">
            <h1>This is the listen page</h1>
        </div>
    )
}

export default Listen